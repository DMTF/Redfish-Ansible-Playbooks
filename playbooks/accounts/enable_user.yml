---
- hosts: myhosts
  connection: local
  name: Enable User
  gather_facts: False

  vars:
  - account_username: user1

  tasks:

  - name: Enable user
    redfish_command:
      category: Accounts
      command: EnableUser
      baseuri: "{{ baseuri }}"
      username: "{{ username }}"
      password: "{{ password }}"
      account_username: "{{ account_username }}"
